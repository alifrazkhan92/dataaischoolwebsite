# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment / secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key
*-key.json
*-credentials.json
credentials.json
service-account*.json
client_secret*.json

# Editor / IDE
.idea/
.vscode/
*.swp

# Google Apps Script source — managed inside the Apps Script IDE.
# Keep locally for reference but do not expose backend logic in the public repo.
google-apps-script/

# Cloudflare Wrangler local cache (contains account id)
cloudflare-worker/.wrangler/
.wrangler/
